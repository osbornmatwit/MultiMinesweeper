package multiminesweeper.connector.events;

public enum EventType {
    CONNECT,
    DISCONNECT,
    CHAT,
    ERROR,
    GAME_OVER,
    BOARD, READY, /*,
    // these types are handled separately
    MOVE_RESULT,
    MOVE */
}
